/**
 * Package that contains the controllers of the controller layer responsible for managing the requests coming from the view, each entity has its own controller.
 */
package it.unimib.insalute.controllers;
